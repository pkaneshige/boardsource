# Sanity CMS Configuration
# Get these values from https://www.sanity.io/manage

# Your Sanity project ID (found in project settings)
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id

# The dataset to use (typically "production" or "development")
NEXT_PUBLIC_SANITY_DATASET=production

# API token for write operations (required for syncing scraped products)
# Generate at: https://www.sanity.io/manage → Project → API → Tokens
# Required permissions: Editor or higher
SANITY_API_TOKEN=your-api-token

# API key for triggering manual product sync
# Generate a secure random string for this value
# Used to authenticate POST requests to /api/sync
SYNC_API_KEY=your-sync-api-key
