import { page } from "./page";
import { announcement } from "./announcement";
import { navigationItem } from "./navigationItem";
import { surfboard } from "./surfboard";
import { priceSnapshot } from "./price-snapshot";
import { priceAlert } from "./price-alert";
import { notification } from "./notification";

export const schemaTypes = [page, announcement, navigationItem, surfboard, priceSnapshot, priceAlert, notification];

export { page, announcement, navigationItem, surfboard, priceSnapshot, priceAlert, notification };
