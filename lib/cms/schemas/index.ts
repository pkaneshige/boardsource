import { page } from "./page";
import { announcement } from "./announcement";
import { navigationItem } from "./navigationItem";

export const schemaTypes = [page, announcement, navigationItem];

export { page, announcement, navigationItem };
